---
description: Terminal usage instructions for the agent.
applyTo: '**'
---

<!-- Tip: Use /create-instructions in chat to generate content with agent assistance -->

Should not use commands that might look to be stuck such as `tail -f` or `head` since they will not terminate and will block the agent from continuing. Instead, use commands that will complete and return output, such as `cat`, or `ls`.